# JDBC 类型转换修复 - 实施任务清单

## 1. Java 核心实现

- [ ] 1.1 在 `DmJdbcBridge.java` 添加 `getColumnValue()` 方法框架
- [ ] 1.2 实现 BLOB 类型处理（`rs.getBytes()` + Base64 编码）
- [ ] 1.3 实现 CLOB/LONGVARCHAR 类型处理（`rs.getString()` + 1MB 截断）
- [ ] 1.4 实现 TIMESTAMP/DATE 类型处理（`rs.getString()` ISO 8601 格式）
- [ ] 1.5 实现 DECIMAL/NUMERIC 类型处理（`rs.getBigDecimal()`）
- [ ] 1.6 添加达梦特有类型支持（LONG, LONG VARCHAR, TEXT 类型名称检查）
- [ ] 1.7 实现错误捕获和降级处理（返回错误字符串 + 记录日志）
- [ ] 1.8 添加 NULL 值检测（`rs.wasNull()` 检查）
- [ ] 1.9 在 `executeQuery()` 方法中集成 `getColumnValue()`
- [ ] 1.10 在 JSON 响应中添加 `columnTypes` 数组

## 2. 大对象大小限制

- [ ] 2.1 定义大小限制常量（BLOB_MAX_SIZE = 10MB, CLOB_MAX_SIZE = 1MB）
- [ ] 2.2 实现 BLOB 大小检测和截断逻辑（添加 `(truncated from <size>MB)` 后缀）
- [ ] 2.3 实现 CLOB 长度检测和截断逻辑（添加 `(truncated)` 后缀）
- [ ] 2.4 添加大小警告日志（记录原始大小和截断信息）

## 3. 错误处理和日志

- [ ] 3.1 添加类型转换异常捕获逻辑
- [ ] 3.2 实现错误字符串格式（`"<类型转换失败: <错误信息>>"`）
- [ ] 3.3 添加警告日志记录（包含表名、列名、JDBC 类型、错误原因）
- [ ] 3.4 实现日志级别控制（转换失败使用 WARN 级别）
- [ ] 3.5 测试错误处理路径（模拟类型转换失败场景）

## 4. 单元测试

- [ ] 4.1 创建测试数据库表（包含 BLOB、CLOB、LONG、TIMESTAMP、DECIMAL 字段）
- [ ] 4.2 编写 BLOB 类型转换测试（空值、小文件、大文件、截断）
- [ ] 4.3 编写 CLOB/LONG 类型转换测试（空值、短文本、长文本、截断）
- [ ] 4.4 编写 TIMESTAMP 类型转换测试（不同日期时间格式）
- [ ] 4.5 编写 DECIMAL 类型转换测试（整数、小数、负数、NULL）
- [ ] 4.6 编写类型转换失败测试（模拟未知类型和异常）
- [ ] 4.7 编写 NULL 值处理测试（各种类型的 NULL 值）
- [ ] 4.8 验证 JSON 序列化正确性（所有类型都能正确序列化）

## 5. 集成测试

- [ ] 5.1 测试 `dm_query` 工具查询包含 BLOB 的表
- [ ] 5.2 测试 `dm_query` 工具查询包含 CLOB 的表
- [ ] 5.3 测试 `dm_get_view_definition` 工具获取视图定义（LONG 类型）
- [ ] 5.4 测试 `dm_describe_table` 工具返回的列信息
- [ ] 5.5 测试 `columnTypes` 元数据正确性
- [ ] 5.6 测试混合类型查询（同时包含多种类型）
- [ ] 5.7 测试错误场景（类型转换失败不影响其他字段）
- [ ] 5.8 测试向后兼容性（旧客户端仍能正常解析响应）

## 6. 文档更新

- [ ] 6.1 更新 `CLAUDE.md` 添加 JDBC 类型转换说明
- [ ] 6.2 创建示例代码展示如何处理 BLOB/CLOB 字段
- [ ] 6.3 在 `README.md` 中添加已知限制说明（大小限制）
- [ ] 6.4 添加故障排查指南（类型转换失败诊断）

## 7. 编译和部署

- [ ] 7.1 编译 Java 代码（`javac -cp 'lib/*' db/DmJdbcBridge.java`）
- [ ] 7.2 验证编译产物存在（`DmJdbcBridge.class`）
- [ ] 7.3 备份当前运行版本的 `.class` 文件
- [ ] 7.4 部署新的 `.class` 文件到生产目录
- [ ] 7.5 重启 Java 进程（如正在运行）

## 8. 验证和监控

- [ ] 8.1 执行基本查询验证（`SELECT 1 FROM DUAL`）
- [ ] 8.2 测试视图定义获取（验证 LONG 类型修复）
- [ ] 8.3 测试 BLOB 字段查询（验证 Base64 编码）
- [ ] 8.4 测试 CLOB 字段查询（验证文本读取）
- [ ] 8.5 检查日志文件（确认无类型转换错误）
- [ ] 8.6 监控内存使用（确认无内存泄漏）
- [ ] 8.7 验证 `columnTypes` 元数据输出

## 9. 性能测试

- [ ] 9.1 测试小 BLOB（< 1MB）查询性能
- [ ] 9.2 测试大 BLOB（接近 10MB）查询性能和 Base64 编码时间
- [ ] 9.3 测试大 CLOB（接近 1MB）查询性能
- [ ] 9.4 对比修复前后的查询响应时间
- [ ] 9.5 压力测试（并发查询包含大对象的表）

## 10. 代码审查

- [ ] 10.1 自查代码质量（命名规范、注释完整性、错误处理）
- [ ] 10.2 确认所有测试通过（单元测试 + 集成测试）
- [ ] 10.3 验证日志级别合理（无敏感信息泄露）
- [ ] 10.4 验证向后兼容性（旧客户端仍可正常使用）
- [ ] 10.5 进行安全审查（无新的安全风险）
- [ ] 10.6 性能审查（确认无明显性能退化）
- [ ] 10.7 文档审查（CLAUDE.md 和示例代码完整）
- [ ] 10.8 回答 Reviewer 反馈并修改代码（如有）

## 11. 清理和收尾

- [ ] 11.1 清理调试日志（生产环境使用 INFO/WARN 级别）
- [ ] 11.2 移除临时测试代码和数据
- [ ] 11.3 优化代码注释（确保关键逻辑有说明）
- [ ] 11.4 更新 Git commit 信息
- [ ] 11.5 合并代码到主分支

---

**任务总数**: 11 个大类，共 82 个任务
