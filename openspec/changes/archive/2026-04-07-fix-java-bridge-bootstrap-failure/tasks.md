## 1. 修复 Java 桥接引导

- [x] 1.1 在 `db/java_bridge.py` 增加 `DmJdbcBridge` 主类产物自检，并在 class 缺失或过期时自动调用 `javac`
- [x] 1.2 保留 `javac`/`java` 启动阶段的真实失败信息，避免桥接错误退化成无上下文文案

## 2. 修复共享客户端错误传播

- [x] 2.1 在 `db/client.py` 增加显式连接保证路径，让 `get_shared_client()` 直接透传桥接启动根因
- [x] 2.2 保持现有 `connect()` 布尔语义，不破坏已有调用和测试

## 3. 回归验证与文档

- [x] 3.1 增加单元测试，覆盖自动编译触发、缺少 `javac`/编译失败报错、共享 client 根因透传
- [x] 3.2 更新 `README.md`，使启动说明与自动编译后的真实前提保持一致
- [x] 3.3 运行相关 pytest 用例验证修复
